<%= form_for [@billboard, @artist] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, required: true %>
  <%= f.label :body %>
  <%= f.text_area :body, required: true %>
  <%= f.submit %>
<% end %>